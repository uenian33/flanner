/* The Material Web components this project uses, bundled into one module.
   Only what is actually on a page is imported: every extra element is bytes
   inlined into every self-contained file. */
import '@material/web/button/filled-button.js';
import '@material/web/button/outlined-button.js';
