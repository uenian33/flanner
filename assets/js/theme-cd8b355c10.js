/* Generated by build_planner.py — the recipe, then the engine. */
window.__FP_THEME_RECIPE__ = {"light":[{"n":"--wash","v":"#FFFFFF"},{"n":"--low","v":"#F8F7F3"},{"n":"--card","v":"#F2F0EB"},{"n":"--card-a","l":95.375901,"c":5.072906},{"n":"--card-f","v":"#F4F0F7"},{"n":"--hover","l":91.923777,"c":4.69343},{"n":"--on","l":10.049135,"c":7.46238},{"n":"--on-var","l":32.388466,"c":7.886323},{"n":"--outline","l":80.960247,"c":9.134767},{"n":"--primary","l":39.937743,"c":36.194709},{"n":"--sec","l":90.199358,"c":21.122943},{"n":"--on-sec","l":16.454136,"c":23.272842},{"n":"--plan","l":28.528802,"c":36.366777},{"n":"--on-plan","l":95.571238,"c":14.688778},{"n":"--sec-a","l":89.670645,"c":16.227477},{"n":"--on-sec-a","v":"#2B1700"},{"n":"--plan-a","v":"#6B4310"},{"n":"--on-plan-a","v":"#FFEEDC"},{"n":"--sec-f","v":"#E8DEF8"},{"n":"--on-sec-f","v":"#1D192B"},{"n":"--plan-f","v":"#4F378B"},{"n":"--on-plan-f","v":"#EADDFF"},{"n":"--dot-m","l":78.668008,"c":39.498375},{"n":"--dot-a","v":"#E5C193"},{"n":"--dot-f","v":"#CFC1E8"},{"n":"--art-m-bg","l":93.369741,"c":17.255402},{"n":"--art-m-1","l":89.948105,"c":39.753301},{"n":"--art-m-ink","l":27.986719,"c":35.870847},{"n":"--art-a-bg","l":93.72365,"c":13.690716},{"n":"--art-a-1","v":"#FFDDB3"},{"n":"--art-a-2","v":"#855318"},{"n":"--art-a-ink","v":"#4A2A02"},{"n":"--art-f-bg","v":"#EFE6FA"},{"n":"--art-f-2","v":"#6750A4"},{"n":"--art-f-ink","v":"#341E73"},{"n":"--state6","l":10.049135,"c":7.46238,"a":".055"},{"n":"--state8","l":10.049135,"c":7.46238,"a":".08"},{"n":"--state10","l":10.049135,"c":7.46238,"a":".10"},{"n":"--scrim","l":7.530717,"c":6.620728,"a":".32"},{"n":"--state9","l":10.049135,"c":7.46238,"a":".09"},{"n":"--heart-cont","l":90.610091,"c":19.593813},{"n":"--on-heart-cont","l":10.003839,"c":16.910156},{"n":"--heart","l":39.63446,"c":52.017991},{"n":"--bar-edge","l":80.960247,"c":9.134767,"a":".6"},{"n":"--state5","l":10.049135,"c":7.46238,"a":".045"},{"n":"--line1","l":87.942537,"c":5.217485},{"n":"--line2","l":93.275226,"c":4.127888},{"n":"--dim-on","l":41.863762,"c":9.395821},{"n":"--tag-edge","l":10.049135,"c":7.46238,"a":".22"},{"n":"--star-chip-on","v":"rgba(255,255,255,.18)"},{"n":"--star-chip","v":"rgba(255,255,255,.6)"},{"n":"--pink-row","l":90.440189,"c":10.715908},{"n":"--pink-on","l":9.900656,"c":31.044177},{"n":"--pink-card","l":96.477857,"c":4.96603},{"n":"--pink-primary","l":39.123591,"c":34.635483},{"n":"--bar","v":"rgba(255,255,255,.94)"},{"n":"--warm-plan","l":32.209438,"c":37.988605},{"n":"--on-warm-plan","l":94.982879,"c":11.144458},{"n":"--state7","l":10.049135,"c":7.46238,"a":".07"},{"n":"--pink-on-var","l":31.188304,"c":25.000563},{"n":"--pink-track","l":83.423953,"c":19.220642},{"n":"--state-primary","l":39.937743,"c":36.194709,"a":".12"},{"n":"--state-sec","l":16.454136,"c":23.272842,"a":".08"}],"dark":[{"n":"--wash","l":6.58465,"c":6.072026},{"n":"--low","l":9.426426,"c":8.143491},{"n":"--card","l":11.441988,"c":7.686023},{"n":"--card-a","v":"#221A11"},{"n":"--card-f","v":"#1C1A22"},{"n":"--hover","l":17.735704,"c":11.64675},{"n":"--on","l":89.997785,"c":7.855793},{"n":"--on-var","l":79.873209,"c":12.498684},{"n":"--dim-on","l":60.296884,"c":10.506094},{"n":"--outline","l":29.91698,"c":14.841765},{"n":"--line1","l":19.87813,"c":14.125103},{"n":"--line2","l":14.902267,"c":11.880161},{"n":"--primary","l":80.057127,"c":39.170498},{"n":"--sec","l":30.067666,"c":35.213386},{"n":"--on-sec","l":89.948105,"c":39.753301},{"n":"--plan","l":89.948105,"c":39.753301},{"n":"--on-plan","l":16.454136,"c":23.272842},{"n":"--heart","l":79.720992,"c":36.905972},{"n":"--heart-cont","l":32.209438,"c":37.988605},{"n":"--on-heart-cont","l":94.982879,"c":11.144458},{"n":"--warm-plan","l":79.720992,"c":36.905972},{"n":"--on-warm-plan","l":10.003839,"c":16.910156},{"n":"--dot-m","l":62.020011,"c":39.544268},{"n":"--dot-a","v":"#9A6C31"},{"n":"--dot-f","v":"#8C7BB8"},{"n":"--sec-a","v":"#4A2E08"},{"n":"--on-sec-a","v":"#FFDDB3"},{"n":"--plan-a","v":"#FFDDB3"},{"n":"--on-plan-a","v":"#2B1700"},{"n":"--sec-f","v":"#362A56"},{"n":"--on-sec-f","v":"#EADDFF"},{"n":"--plan-f","v":"#D6C4F5"},{"n":"--on-plan-f","v":"#1D192B"},{"n":"--art-m-bg","l":18.008304,"c":17.627291},{"n":"--art-m-1","l":41.790683,"c":35.527473},{"n":"--art-m-ink","l":89.948105,"c":39.753301},{"n":"--art-a-bg","v":"#2E2113"},{"n":"--art-a-1","v":"#6B4A20"},{"n":"--art-a-2","v":"#E0B183"},{"n":"--art-a-ink","v":"#FFDDB3"},{"n":"--art-f-bg","v":"#221C2E"},{"n":"--art-f-2","v":"#C6B2F0"},{"n":"--art-f-ink","v":"#EADDFF"},{"n":"--pink-card","l":7.370146,"c":12.107732},{"n":"--pink-row","l":15.079279,"c":19.743701},{"n":"--pink-track","l":21.214234,"c":25.36212},{"n":"--pink-primary","l":80.095724,"c":24.322963},{"n":"--pink-on","l":90.03491,"c":14.613018},{"n":"--pink-on-var","l":78.056846,"c":21.873775},{"n":"--state5","l":89.997785,"c":7.855793,"a":".06"},{"n":"--state6","l":89.997785,"c":7.855793,"a":".07"},{"n":"--state7","l":89.997785,"c":7.855793,"a":".08"},{"n":"--state8","l":89.997785,"c":7.855793,"a":".09"},{"n":"--state9","l":89.997785,"c":7.855793,"a":".11"},{"n":"--state10","l":89.997785,"c":7.855793,"a":".12"},{"n":"--tag-edge","l":89.997785,"c":7.855793,"a":".28"},{"n":"--state-sec","l":6.58465,"c":6.072026,"a":".10"},{"n":"--state-primary","l":80.057127,"c":39.170498,"a":".14"},{"n":"--bar","l":6.58465,"c":6.072026,"a":".92"},{"n":"--bar2","l":6.58465,"c":6.072026,"a":".9"},{"n":"--glass","l":6.58465,"c":6.072026,"a":".78"},{"n":"--star-chip","l":6.58465,"c":6.072026,"a":".4"},{"n":"--star-chip-on","l":6.58465,"c":6.072026,"a":".22"},{"n":"--scrim","v":"rgba(0,0,0,.6)"},{"n":"--bar-edge","l":29.91698,"c":14.841765,"a":".7"}],"tint":{"n":"--hero-tint","l":89.948105,"c":39.753301},"duo":{"n":"--hero-duo","l":39.937743,"c":36.194709},"stages":[{"k":"bg","l":[21,90],"d":[24,30],"always":false},{"k":"fg","l":[23,16],"d":[30,85],"always":false},{"k":"dot","l":[39,79],"d":[30,62],"always":false},{"k":"planBg","l":[36,28],"d":[30,85],"always":false},{"k":"planFg","l":[15,95],"d":[23,16],"always":false},{"k":"artBg","l":[12,93],"d":[14,17],"always":true},{"k":"art1","l":[28,89],"d":[26,41],"always":true},{"k":"art2","l":[36,40],"d":[30,81],"always":true},{"k":"art3","l":[39,79],"d":[25,61],"always":true},{"k":"artInk","l":[40,27],"d":[30,89],"always":true},{"k":"hero","l":[18,95],"d":[30,88],"always":false},{"k":"qBg","l":[10,94],"d":[12,24],"always":false},{"k":"qTone","l":[16,86],"d":[16,34],"always":false},{"k":"qInk","l":[26,52],"d":[22,66],"always":false}]};
/* ── the scheme, in the browser ─────────────────────────
   The planners are one design drawn in one green, and each festival turns that
   green to its own. Until now the turning happened in Python: `theme_css` read
   every tone the design had chosen and re-emitted it at the festival's hue, and
   the answer was baked into that festival's page. That works, and it costs a
   page per festival.

   This is the same arithmetic, in JavaScript, so a planner can be handed an
   accent and theme itself. `scripts/m3color.py` is the reference implementation
   and this is checked against it token for token — see `tools/theme_parity.py`,
   which fails the build if the two ever disagree.

   Only the hue moves. Every token keeps the lightness and the chroma the design
   gave it, so every contrast pair the design was drawn against still holds: the
   page changes colour without changing tone. That is what makes this safe to do
   at runtime for a festival nobody has audited by hand. */
(() => {
  'use strict';

  const WHITE = [0.95047, 1.0, 1.08883];
  const E = 216 / 24389, K = 24389 / 27;

  const toLinear = (c) => (c <= 0.04045 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4));
  /* Negative and above-one values keep the linear branch, which is what stops
     a fractional power of a negative appearing here at all — the same reason
     the Python has no special case for it. */
  const toSrgb = (c) => (c <= 0.0031308 ? 12.92 * c : 1.055 * Math.pow(c, 1 / 2.4) - 0.055);

  function hexToLab(hex) {
    const h = hex.replace('#', '');
    const r = toLinear(parseInt(h.slice(0, 2), 16) / 255);
    const g = toLinear(parseInt(h.slice(2, 4), 16) / 255);
    const b = toLinear(parseInt(h.slice(4, 6), 16) / 255);
    const x = (0.4124564 * r + 0.3575761 * g + 0.1804375 * b) / WHITE[0];
    const y = (0.2126729 * r + 0.7151522 * g + 0.0721750 * b) / WHITE[1];
    const z = (0.0193339 * r + 0.1191920 * g + 0.9503041 * b) / WHITE[2];
    const f = (t) => (t > E ? Math.cbrt(t) : (K * t + 16) / 116);
    const fx = f(x), fy = f(y), fz = f(z);
    return [116 * fy - 16, 500 * (fx - fy), 200 * (fy - fz)];
  }

  function labToRgb(L, a, b) {
    const fy = (L + 16) / 116, fx = fy + a / 500, fz = fy - b / 200;
    const g = (t) => (t * t * t > E ? t * t * t : (116 * t - 16) / K);
    const x = g(fx) * WHITE[0], y = g(fy) * WHITE[1], z = g(fz) * WHITE[2];
    return [
      toSrgb(3.2404542 * x - 1.5371385 * y - 0.4985314 * z),
      toSrgb(-0.9692660 * x + 1.8760108 * y + 0.0415560 * z),
      toSrgb(0.0556434 * x - 0.2040259 * y + 1.0572252 * z),
    ];
  }

  const inGamut = (rgb) => rgb.every((c) => c >= -1e-4 && c <= 1 + 1e-4);

  /* Python rounds half to even and JavaScript rounds half up, which is one
     value in 255 and therefore a different hex on the boundary. The reference
     is Python's, so this is Python's. */
  function roundHalfEven(x) {
    const f = Math.floor(x), d = x - f;
    if (d > 0.5) return f + 1;
    if (d < 0.5) return f;
    return f % 2 === 0 ? f : f + 1;
  }

  const hex2 = (c) => {
    const v = Math.max(0, Math.min(255, roundHalfEven(c * 255)));
    return v.toString(16).padStart(2, '0');
  };

  /* One tone of a palette: this lightness, this hue, and as much of the chroma
     asked for as the sRGB gamut will take — found by bisection, twenty-four
     times, which is the same number of steps the Python takes so the two land
     on the same value rather than merely near it. */
  function tone(hueDeg, chroma, t) {
    const h = (hueDeg * Math.PI) / 180, cos = Math.cos(h), sin = Math.sin(h);
    let lo = 0, hi = chroma;
    for (let i = 0; i < 24; i++) {
      const mid = (lo + hi) / 2;
      if (inGamut(labToRgb(t, mid * cos, mid * sin))) lo = mid; else hi = mid;
    }
    const rgb = labToRgb(t, lo * cos, lo * sin);
    return '#' + hex2(rgb[0]) + hex2(rgb[1]) + hex2(rgb[2]);
  }

  /* A recipe entry is what the build worked out once and no page needs to work
     out again: the lightness and chroma the design chose, and the alpha where
     the value was an rgba(). Applying a hue to it is the whole of theming. */
  function fromRecipe(e, hue) {
    if (e.v !== undefined) return e.v;          // left as the design had it
    const t = tone(hue, e.c, e.l);
    if (e.a === undefined) return t;
    const r = parseInt(t.slice(1, 3), 16), g = parseInt(t.slice(3, 5), 16), b = parseInt(t.slice(5, 7), 16);
    return 'rgba(' + r + ',' + g + ',' + b + ',' + e.a + ')';
  }

  const decls = (list, hue) =>
    list.map((e) => e.n + ':' + fromRecipe(e, hue)).join(';');

  /* The stages, spaced by the golden angle from the festival's own hue: ten
     stages in equal steps are 36 degrees apart, which at these tones is one
     green beside another. */
  function stageDecls(roles, n, hue, dark) {
    const out = [];
    for (let i = 0; i < n; i++) {
      const h = (hue + i * 137.507) % 360;
      for (const r of roles) {
        const pair = dark || r.always ? r.d : r.l;
        out.push('--st' + i + '-' + r.k.toLowerCase() + ':' + tone(h, pair[0], pair[1]));
      }
    }
    return out.join(';');
  }

  /* The whole scheme for one accent, as the two blocks the page already
     expects — so what this writes is what the build used to write. */
  function schemeCss(recipe, accent, stages) {
    const lab = hexToLab(accent);
    const hue = ((Math.atan2(lab[2], lab[1]) * 180) / Math.PI + 360) % 360;
    return [
      ':root:not([data-theme="dark"]){' + decls(recipe.light, hue) + '}',
      '[data-theme="dark"]{' + decls(recipe.dark, hue) + '}',
      '.fest{--hero-tint:' + tone(hue, recipe.tint.c, recipe.tint.l) +
        ';--hero-duo:' + tone(hue, recipe.duo.c, recipe.duo.l) + '}',
      ':root{' + stageDecls(recipe.stages, stages, hue, false) + '}',
      '[data-theme="dark"]{' + stageDecls(recipe.stages, stages, hue, true) + '}',
    ].join('\n');
  }

  /* Apply a festival's accent to this document.

     The page already carries the same scheme, written by the build, so that
     the first paint is themed and not a green flash. This does not fight it:
     it writes the same declarations into a later block, which is a no-op for
     a festival the build knew about and the whole theme for one it did not.
     That is the point — a planner can now be handed an accent it has never
     been built for and draw itself correctly. */
  function apply(accent, stages, doc) {
    const d = doc || document;
    let el = d.getElementById('fp-theme');
    if (!el) {
      el = d.createElement('style');
      el.id = 'fp-theme';
      d.head.appendChild(el);
    }
    el.textContent = schemeCss(RECIPE, accent, stages);
    return el.textContent;
  }

  /* The recipe is written in beside this file by the build — the tokens the
     design chose, with the hue taken out of them. */
  const RECIPE = window.__FP_THEME_RECIPE__ || null;

  window.FlannerTheme = { tone, hexToLab, labToRgb, schemeCss, stageDecls, fromRecipe, apply,
                          get recipe() { return RECIPE; } };

  /* A planner says which festival it is on the root element. Nothing else has
     to be wired: the page is themed from its own dataset the moment this
     runs, whether or not the build had ever heard of that festival. */
  const root = document.documentElement;
  const accent = root.getAttribute('data-accent');
  if (RECIPE && accent) {
    apply(accent, parseInt(root.getAttribute('data-stages') || '0', 10) || 0);
  }
})();
